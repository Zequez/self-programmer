{
  "scripts": {
    "run": {
      "cmd": "deno run --allow-net --allow-read --allow-run --unstable ./main.ts start",
      "desc": "Run the app",
      "watch": false
    }
  },

  "watcher": {
    "match": ["main.ts"],
    "skip": ["*/.git/*", "*/dist/*"]
  }
}
