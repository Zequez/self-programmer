type Agent = {};

export type Perspective = {
  id: string;
  app: string;
  name: string;
  input: Record<string, unknown>;
  output: Record<string, unknown>;
};

// class Perspective {
//   id: string;
// }
